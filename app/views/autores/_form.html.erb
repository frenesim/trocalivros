<%= form_for(@autore) do |f| %>
  <% if @autore.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@autore.errors.count, "error") %> prohibited this autore from being saved:</h2>

      <ul>
      <% @autore.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
