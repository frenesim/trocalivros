- model_class = Autore
.page-header
  %h1=t '.title', :default => model_class.model_name.human

%p
  %strong= model_class.human_attribute_name(:nome) + ':'
  %br
  = @autore.nome

.form-actions
  = link_to t('.back', :default => t("helpers.links.back")), autores_path, :class => 'btn'
  = link_to t('.edit', :default => t("helpers.links.edit")), edit_autore_path(@autore), :class => 'btn'
  = link_to t('.destroy', :default => t("helpers.links.destroy")), autore_path(@autore), :method => "delete", :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-danger'
