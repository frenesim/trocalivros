.control-group
  .controls
    =image_tag p.object.image_url(:thumb)
    %span.btn.btn-danger.fileinput-button
      %span
      =link_to 'Apagar', photo_path(p.object.id), method: 'delete'

//=p.file_field :image, multiple: true